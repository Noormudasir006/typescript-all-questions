//adding comments: choose two of the programs you've written and add at least on coment to each if you don'tt ghave anything specific to write because your programms are too simple at this point just add your name and the current date at the top of each program file then write one sentence describing what the program does

let a1 = 20;
let b1 = 30;
//addition
console.log(`${a1}+${b1}=${a1+b1}`)
//subtraction
console.log(`${a1}-${b1}=${a1-b1}`)
//multiplication
console.log(`${a1}*${b1}=${a1*b1}`)
//division
console.log(`${a1}/${b1}=${a1/b1}`)
